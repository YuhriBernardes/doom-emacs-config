# -*- mode: snippet -*-
# name: React Functional component
# key: rfc
# --
import React from 'react';

const ${1:`(car (last (split-string (file-name-directory buffer-file-name) "/") 2))`} = (props) => (
  $0
);

export default $1;